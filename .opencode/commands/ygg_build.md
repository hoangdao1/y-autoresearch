Read `.opencode/skills/ygg_build/SKILL.md`.

Extract from user input:
- `--iterations N` (default: 5)
- `--output <dir>` (default: `generated/`)
- Remaining text → description

If description is missing or unclear, ask in one batch:
1. What should the app do?
2. What agents / tools are needed?
3. Sequential pipeline, parallel workers, or conditional routing?

Then execute the full build protocol (Phases 1–4) as defined in the skill.

EXECUTE IMMEDIATELY — do not deliberate before reading the protocol.
